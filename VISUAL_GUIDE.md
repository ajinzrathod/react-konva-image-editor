# 🎨 Image Editor - Visual Overview

## Application Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    IMAGE EDITOR                             │
│                  React + Konva.js                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  UPLOAD SCREEN   │
                    │ ┌──────────────┐ │
                    │ │ Choose Image │ │
                    │ │  Max: 5MB    │ │
                    │ └──────────────┘ │
                    └──────────────────┘
                              │
                              ▼
                   ┌────────────────────────┐
                   │ Image Loaded & Scaled  │
                   │ Auto-fit to viewport   │
                   │ (STAGE: 800x500)       │
                   └────────────────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │   EDITOR SCREEN      │
                    │ ┌──────────────────┐ │
                    │ │ Aspect Ratio:    │ │
                    │ │ Free│Square│Rect │ │
                    │ └──────────────────┘ │
                    │ ┌──────────────────┐ │
                    │ │  [Konva Canvas]  │ │  ← Image + Crop Box
                    │ │  with 8 handles  │ │     with 8 handles
                    │ └──────────────────┘ │
                    │ ┌──────────────────┐ │
                    │ │ Zoom/Pan Controls│ │
                    │ │−  100%  +  Fit   │ │
                    │ └──────────────────┘ │
                    │ ┌──────────────────┐ │
                    │ │ [Preview Result] │ │
                    │ │ [Cancel]         │ │
                    │ └──────────────────┘ │
                    └──────────────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │ Canvas Merge Process │
                    │ 1. Extract crop      │
                    │ 2. Scale to region   │
                    │ 3. Merge with templ. │
                    │ 4. PNG export ready  │
                    └──────────────────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │  PREVIEW SCREEN      │
                    │ ┌──────────────────┐ │
                    │ │  [Merged Image]  │ │
                    │ └──────────────────┘ │
                    │ ┌──────────────────┐ │
                    │ │ [Download] [Fix] │ │
                    │ └──────────────────┘ │
                    └──────────────────────┘
```

## Crop Box Interaction

```
Interactive Crop Box with 8 Handles:

        ◼─────────────────────◼
        │                     │
        │ Drag to move box    │
        │                     │
    ◼───┼─────────────────────┼───◼
    │   │                     │   │
    │   │   [Image Region]    │   │
    │   │                     │   │
    ◼───┼─────────────────────┼───◼
        │                     │
        ◼─────────────────────◼

Legend:
◼ = Resize handle (8 total)
  ┌─ = Crop box outline
  │ = Drag inside to move
  └─ = Dashed border


Handles:
┌─────────────────┐
│TL      T      TR│
│                 │
│L              R │
│                 │
│BL      B      BR│
└─────────────────┘

Actions:
- TL, TR, BL, BR = Resize diagonally
- T, B, L, R     = Resize one direction
- Inside box     = Move entire box
```

## Image State Lifecycle

```
User uploads image
        │
        ▼
┌─────────────────────────────┐
│ Calculate fit scale:        │
│ scaleX = 800 / imgWidth     │
│ scaleY = 500 / imgHeight    │
│ fitScale = Min(scaleX, scaleY, 1)
└─────────────────────────────┘
        │
        ▼
┌─────────────────────────────┐
│ Center in viewport:         │
│ x = (800 - width*scale) / 2 │
│ y = (500 - height*scale) / 2│
└─────────────────────────────┘
        │
        ▼
┌─────────────────────────────┐
│ imageState = {              │
│   x: centered_x,            │
│   y: centered_y,            │
│   scale: fitScale           │
│ }                           │
└─────────────────────────────┘
        │
        ▼
    User interacts:
    - Zoom: scale * 1.1
    - Pan: x/y changes
    - Fit: Reset to fitScale
```

## Canvas Merge Algorithm

```
User Image             Template Image
     │                      │
     ├─ Crop Box ──────────┐│
     │  Region             ││
     │                     ││
     ▼                     ▼▼
  ┌──────┐        ┌─────────────────┐
  │ Crop │        │    Template     │
  │ Area │        │   with region   │
  └──────┘        └─────────────────┘
     │                     │
     ├─ Extract ──────────┤
     │ cropped region      │
     │                     │
     ▼                     │
  ┌──────┐                 │
  │Cropped               │
  │ Canvas│               │
  └──────┘                │
     │                     │
     ├─ Calculate scale:  │
     │  scale = min(      │
     │    regionW/cropW,  │
     │    regionH/cropH   │
     │  )                 │
     │                     │
     ├─ Center offset:    │
     │  offsetX/Y = ...   │
     │                     │
     ▼                     ▼
  ┌─────────────────────────────┐
  │      Merged Image Canvas    │
  │                             │
  │  [Template with crop  ▼]    │
  │  [placed at region]         │
  │                             │
  └─────────────────────────────┘
     │
     ▼
  PNG Export ──► Browser Download
```

## UI State Machine

```
STATE 1: UPLOAD
┌──────────────────────┐
│  Choose Image        │  ──upload──>
│  File Input          │
└──────────────────────┘
         ▲              
         │              
         └──── reset ────
              from Edit or Preview

STATE 2: EDIT
┌──────────────────────────────┐
│ Aspect Controls              │  ──preview──>
│ Konva Canvas                 │
│ (Image + Crop Box)           │
│ Zoom/Pan Controls            │
│ [Preview] [Cancel]           │
└──────────────────────────────┘
         ▲              
         │              
         └──── cancel ────
              back to Upload

STATE 3: PREVIEW
┌──────────────────────┐
│ Merged Image         │  ──download──> File
│ [Download] [Retake]  │
└──────────────────────┘
         │
         └──── retake ──>  Back to Edit
```

## Keyboard & Mouse Interactions

```
MOUSE INTERACTIONS:
┌─────────────────────────────────────────┐
│ Scroll Wheel    │ Zoom In/Out (0.5-3x) │
│ Drag Image      │ Pan around           │
│ Drag Corner     │ Resize diagonally    │
│ Drag Edge       │ Resize one direction │
│ Drag Inside Box │ Move crop box        │
│ Drag Outside    │ Nothing              │
└─────────────────────────────────────────┘

BUTTON INTERACTIONS:
┌─────────────────────────────────────────┐
│ [Free]          │ Unlock aspect ratio  │
│ [Square]        │ Lock 1:1 ratio       │
│ [Rectangle]     │ Lock 4:3 ratio       │
│ [− Zoom Out]    │ Scale down 10%       │
│ [+ Zoom In]     │ Scale up 10%         │
│ [Fit]           │ Reset to viewport    │
│ [Preview]       │ Merge & show result  │
│ [Cancel]        │ Back to upload       │
│ [Download]      │ Export as PNG        │
│ [Retake]        │ Go back to edit      │
└─────────────────────────────────────────┘
```

## Responsive Breakpoints

```
Desktop (800px+)           Tablet (600-800px)      Mobile (< 600px)
┌────────────────┐         ┌──────────────┐       ┌───────────────┐
│   Container    │         │  Container   │       │   Container   │
│  max-width     │         │  max-width   │       │  full width   │
│  1000px        │         │  600px       │       │  padding: 20px│
│ padding: 40px  │         │ padding: 30px│       │               │
│                │         │              │       │               │
│ [Buttons]      │         │ [Buttons]    │       │ Stack buttons │
│ ├─ Horizontal  │         │ ├─ Horizontal│       │ ├─ Vertical   │
│ └─ 2 columns   │         │ └─ 2 columns │       │ └─ Full width │
└────────────────┘         └──────────────┘       └───────────────┘

Crop Editor:
Desktop: 800x500           Tablet: 600x400         Mobile: 300x250
```

## Tech Stack Visualization

```
┌───────────────────────────────────────────────┐
│               React 19 App                    │
│  (Functional Components + Hooks)              │
│                                               │
│  ┌─────────────────────────────────────────┐  │
│  │         Component State                 │  │
│  │ • uploadedImage (Image object)          │  │
│  │ • imageState (x, y, scale)              │  │
│  │ • cropBox (x, y, width, height)         │  │
│  │ • aspectRatio (null | 1 | 0.75)         │  │
│  │ • mergedImage (PNG data URL)            │  │
│  └─────────────────────────────────────────┘  │
│                                               │
│  ┌─────────────────────────────────────────┐  │
│  │      Konva.js Canvas Rendering          │  │
│  │ • Stage (800x500)                       │  │
│  │ • Layer (contains all objects)          │  │
│  │ • KonvaImage (user image)               │  │
│  │ • Rect shapes (crop box + handles)      │  │
│  └─────────────────────────────────────────┘  │
│                                               │
│  ┌─────────────────────────────────────────┐  │
│  │    HTML5 Canvas (Hidden)                │  │
│  │ • 2D context                            │  │
│  │ • drawImage() calls                     │  │
│  │ • toDataURL() export                    │  │
│  └─────────────────────────────────────────┘  │
│                                               │
│  ┌─────────────────────────────────────────┐  │
│  │       React Styling (CSS3)              │  │
│  │ • Flexbox layouts                       │  │
│  │ • Responsive media queries              │  │
│  │ • Smooth transitions                    │  │
│  └─────────────────────────────────────────┘  │
└───────────────────────────────────────────────┘
                          │
                ┌─────────┴──────────┐
                ▼                    ▼
         Vite Dev Server      Browser Runtime
         (Hot reload)         (Canvas rendering)
```

---

**Created**: January 31, 2026  
**Status**: ✅ Ready to Use
